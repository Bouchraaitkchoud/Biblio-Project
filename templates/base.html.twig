<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Bibliothèque UIASS{% endblock %}</title>
    <link rel="icon" href="/images/logo.png">

    {% block stylesheets %}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        <link rel="stylesheet" href="{{ asset('css/admin-sidebar.css') }}?v={{ 'now'|date('U') }}">
        <style>
            body {
                background: #fff !important;
                font-family: 'Georgia', serif;
                color: #3e3e3c;
                min-height: 100vh;
            }

            header.library-header {
                background-color: #ebe3d5;
                border-bottom: 1px solid #d9c9a8;
                color: #5a4633;
                padding: 1rem 2rem;
                display: flex;
                align-items: center;
                position: relative;
                min-height: 80px;
            }

            .library-logo {
                width: 50px;
                height: 50px;
                margin-right: 1rem;
            }

            .library-title {
                font-family: 'Merriweather', serif;
                font-size: 1.8rem;
                font-weight: bold;
                color: #5a4633;
            }

            main.library-main {
                max-width: 1100px;
                margin: 2rem auto;
                background-color: #fffefb;
                border: 1px solid #e1d8c3;
                border-radius: 10px;
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.03);
                padding: 2rem;
            }

            h1, h2, h3, h4, h5 {
                font-family: 'Merriweather', serif;
                color: #5a4633;
            }

            a, .btn-link {
                color: #6b4f2c;
                text-decoration: none;
            }

            a:hover, .btn-link:hover {
                color: #9b7e46;
            }

            button, .btn {
                background-color: #9b7e46;
                color: #fffdf3;
                border: none;
                border-radius: 5px;
                padding: 0.45rem 1rem;
                font-family: 'Georgia', serif;
                transition: background-color 0.2s;
            }

            button:hover, .btn:hover {
                background-color: #7d6435;
            }

            footer.library-footer {
                background-color: #f2ede1;
                color: #5a4633;
                text-align: center;
                padding: 1rem;
                font-size: 0.95rem;
                border-top: 1px solid #d9c9a8;
                margin-top: 2rem;
            }

            .alert-success {
                background-color: #fefbe9;
                border-left: 5px solid #9b7e46;
                color: #4e442f;
            }
            
            .user-nav {
                position: absolute;
                right: 1rem;
                top: 50%;
                transform: translateY(-50%);
                display: flex;
                gap: 0.75rem;
                align-items: center;
                flex-wrap: nowrap;
                max-width: calc(100% - 300px);
            }
            
            .user-greeting {
                color: #5a4633;
                padding: 0.4rem 0.8rem;
                background: rgba(178, 144, 62, 0.1);
                border-radius: 5px;
                font-size: 0.85em;
                display: flex;
                align-items: center;
                gap: 0.4rem;
                border: 1px solid rgba(178, 144, 62, 0.3);
                white-space: nowrap;
            }
            
            .user-greeting i {
                color: #b2903e;
            }
            
            .nav-link {
                color: #5a4633;
                text-decoration: none;
                padding: 0.5rem 1rem;
                border-radius: 5px;
                transition: all 0.3s ease;
                font-weight: 500;
                display: flex;
                align-items: center;
                gap: 0.5rem;
            }
            
            .nav-link:hover {
                background: rgba(90, 70, 51, 0.1);
                color: #5a4633;
            }
            
            .nav-link.logout-btn {
                background: linear-gradient(135deg, #dc3545, #c82333);
                color: white !important;
                border: none;
                font-weight: 600;
            }
            
            .nav-link.logout-btn:hover {
                background: linear-gradient(135deg, #c82333, #bd2130);
                color: white !important;
                transform: translateY(-2px);
                box-shadow: 0 4px 8px rgba(220, 53, 69, 0.3);
            }
            
            .cart-link {
                background: linear-gradient(135deg, #b2903e, #9b7e46);
                color: white !important;
            }
            
            .cart-link:hover {
                background: linear-gradient(135deg, #9b7e46, #7d6435);
                color: white !important;
            }
            
            .cart-badge {
                background: #dc3545;
                color: white;
                border-radius: 50%;
                padding: 0.2rem 0.5rem;
                font-size: 0.7rem;
                font-weight: bold;
                margin-left: 0.3rem;
                min-width: 1.2rem;
                text-align: center;
                display: inline-block;
            }
            
            .cart-link-enhanced {
                position: relative;
                display: flex !important;
                align-items: center;
            }
            
            /* Responsive navigation */
            @media (max-width: 1600px) {
                .user-nav {
                    gap: 0.5rem;
                }
                .nav-link {
                    padding: 0.4rem 0.8rem;
                    font-size: 0.9em;
                }
            }
            
            @media (max-width: 1200px) {
                .user-greeting strong {
                    display: inline-block;
                    max-width: 100px;
                    overflow: hidden;
                    text-overflow: ellipsis;
                    white-space: nowrap;
                }
            }
            
            @media (max-width: 768px) {
                .user-greeting {
                    display: none;
                }
                .nav-link {
                    padding: 0.5rem;
                }
            }
        </style>
    {% endblock %}
</head>
<body>
    <div class="container">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-3">{{ message }}</div>
        {% endfor %}
    </div>

    {% block body %}
        {% if app.request.get('_route') is defined and app.request.get('_route') starts with 'admin_' %}
            <div class="admin-layout">
                <nav class="admin-sidebar">
                    <div class="sidebar-brand">
                        <h3><i class="fas fa-book-reader"></i> Admin Panel</h3>
                    </div>
                    <div class="sidebar-links">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('admin_dashboard') }}" class="admin-nav-link{% if app.request.get('_route') == 'admin_dashboard' %} active{% endif %}">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>Dashboard</span>
                            </a>
                        {% else %}
                            <a href="{{ path('admin_limited_panel') }}" class="admin-nav-link{% if app.request.get('_route') == 'admin_limited_panel' %} active{% endif %}">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>Mon Panel</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_COMMANDES') %}
                            <a href="{{ path('admin_orders_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_orders' %} active{% endif %}">
                                <i class="fas fa-clipboard-list"></i>
                                <span>Commandes</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_RETOURS') %}
                            <a href="{{ path('admin_returns_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_returns' %} active{% endif %}">
                                <i class="fas fa-undo"></i>
                                <span>Retours</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_DISCIPLINES') %}
                            <a href="{{ path('admin_disciplines_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_disciplines' %} active{% endif %}">
                                <i class="fas fa-sitemap"></i>
                                <span>Disciplines</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('admin_locations_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_locations' %} active{% endif %}">
                                <i class="fas fa-map-marker-alt"></i>
                                <span>Emplacements</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_AUTEURS') %}
                            <a href="{{ path('admin_authors_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_authors' %} active{% endif %}">
                                <i class="fas fa-user-edit"></i>
                                <span>Auteurs</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_EDITEURS') %}
                            <a href="{{ path('admin_publishers_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_publishers' %} active{% endif %}">
                                <i class="fas fa-building"></i>
                                <span>Éditeurs</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_LIVRES') %}
                            <a href="{{ path('admin_books_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_books' or app.request.get('_route') starts with 'admin_exemplaire' %} active{% endif %}">
                                <i class="fas fa-book"></i>
                                <span>Livres</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_UTILISATEURS') %}
                            <a href="{{ path('admin_users_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_users' %} active{% endif %}">
                                <i class="fas fa-user-shield"></i>
                                <span>Utilisateurs</span>
                            </a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GERER_LECTEURS') %}
                            <a href="{{ path('admin_lecteurs_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_lecteurs' %} active{% endif %}">
                                <i class="fas fa-user-graduate"></i>
                                <span>Lecteurs</span>
                            </a>
                        {% endif %}
                        <a href="{{ path('main_site') }}" class="admin-nav-link">
                            <i class="fas fa-globe"></i>
                            <span>Site Principal</span>
                        </a>
                    </div>
                    <div class="sidebar-user">
                        <span><i class="fas fa-user-circle"></i> {{ app.user ? app.user.prenom ~ ' ' ~ app.user.nom : 'Admin' }}</span>
                        <a href="{{ path('app_logout') }}" class="logout-link">
                            <i class="fas fa-sign-out-alt"></i>
                            <span>Déconnexion</span>
                        </a>
                    </div>
                </nav>
                <div class="dashboard-main-content">
                    {% block content %}{% endblock %}
                </div>
            </div>
        {% else %}
        {# Les pages lecteur ajouteront leur propre navigation ici via block('content') #}
        {{ block('content') }}
        {% endif %}
        {% if not (app.request.get('_route') is defined and app.request.get('_route') starts with 'admin_') %}
        <footer class="library-footer">
            &copy; {{ "now"|date("Y") }} Bibliothèque UIASS. Tous droits réservés.
        </footer>
        {% endif %}
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        <script>
            // Global cart functions for all pages - attach to window object
            window.updateCartBadge = function(count = null) {
                const badge = document.querySelector('.cart-badge');
                const cartLink = document.querySelector('.cart-link');
                
                if (count === null) {
                    // Get current count and decrement
                    if (badge) {
                        count = parseInt(badge.textContent) - 1;
                    } else {
                        count = 0;
                    }
                }
                
                if (count <= 0) {
                    if (badge) {
                        badge.remove();
                    }
                } else {
                    if (badge) {
                        badge.textContent = count;
                    } else if (cartLink) {
                        // Create new badge
                        const newBadge = document.createElement('span');
                        newBadge.className = 'cart-badge';
                        newBadge.id = 'cart-badge';
                        newBadge.textContent = count;
                        cartLink.appendChild(newBadge);
                    }
                }
            };
            
            window.addToCartGlobal = function(bookId, button) {
                console.log('addToCartGlobal called with:', bookId, button);
                
                const originalContent = button.innerHTML;
                button.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Ajout...';
                button.disabled = true;
                
                fetch(`/add-to-cart/${bookId}`, {
                    method: 'POST',
                    headers: {
                        'X-Requested-With': 'XMLHttpRequest',
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => {
                    console.log('Response received:', response);
                    return response.json();
                })
                .then(data => {
                    console.log('Data received:', data);
                    if (data.success) {
                        // Update badge
                        const badge = document.querySelector('.cart-badge');
                        const currentCount = badge ? parseInt(badge.textContent) : 0;
                        window.updateCartBadge(currentCount + 1);
                        
                        // Visual feedback
                        button.innerHTML = '<i class="fas fa-check"></i> Ajouté !';
                        button.style.backgroundColor = '#28a745';
                        
                        setTimeout(() => {
                            button.innerHTML = originalContent;
                            button.disabled = false;
                            button.style.backgroundColor = '';
                        }, 2000);
                    } else {
                        button.innerHTML = originalContent;
                        button.disabled = false;
                        alert(data.message || 'Erreur lors de l\'ajout au panier');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    button.innerHTML = originalContent;
                    button.disabled = false;
                    alert('Une erreur est survenue: ' + error.message);
                });
            };
            
            // Ensure proper Turbo navigation
            document.addEventListener('turbo:load', function() {
                // Re-initialize any components that need it after SPA navigation
                console.log('Turbo page loaded:', window.location.pathname);
            });
            
            // Handle external links that should force full page reload
            document.addEventListener('turbo:before-visit', function(event) {
                const url = new URL(event.detail.url);
                // Force full reload for admin-to-main or main-to-admin navigation
                if ((url.pathname.startsWith('/admin') && !window.location.pathname.startsWith('/admin')) ||
                    (!url.pathname.startsWith('/admin') && window.location.pathname.startsWith('/admin'))) {
                    event.preventDefault();
                    window.location.href = event.detail.url;
                }
            });
        </script>
    {% endblock %}
</body>
</html>
