<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Bibliothèque UIASS{% endblock %}</title>
    <link rel="icon" href="/images/logo.png">

    {% block stylesheets %}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        <style>
            body {
                background: #fff !important;
                font-family: 'Georgia', serif;
                color: #3e3e3c;
                min-height: 100vh;
            }

            header.library-header {
                background-color: #ebe3d5;
                border-bottom: 1px solid #d9c9a8;
                color: #5a4633;
                padding: 1rem 2rem;
                display: flex;
                align-items: center;
            }

            .library-logo {
                width: 50px;
                height: 50px;
                margin-right: 1rem;
            }

            .library-title {
                font-family: 'Merriweather', serif;
                font-size: 1.8rem;
                font-weight: bold;
                color: #5a4633;
            }

            main.library-main {
                max-width: 1100px;
                margin: 2rem auto;
                background-color: #fffefb;
                border: 1px solid #e1d8c3;
                border-radius: 10px;
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.03);
                padding: 2rem;
            }

            h1, h2, h3, h4, h5 {
                font-family: 'Merriweather', serif;
                color: #5a4633;
            }

            a, .btn-link {
                color: #6b4f2c;
                text-decoration: none;
            }

            a:hover, .btn-link:hover {
                color: #9b7e46;
            }

            button, .btn {
                background-color: #9b7e46;
                color: #fffdf3;
                border: none;
                border-radius: 5px;
                padding: 0.45rem 1rem;
                font-family: 'Georgia', serif;
                transition: background-color 0.2s;
            }

            button:hover, .btn:hover {
                background-color: #7d6435;
            }

            footer.library-footer {
                background-color: #f2ede1;
                color: #5a4633;
                text-align: center;
                padding: 1rem;
                font-size: 0.95rem;
                border-top: 1px solid #d9c9a8;
                margin-top: 2rem;
            }

            .alert-success {
                background-color: #fefbe9;
                border-left: 5px solid #9b7e46;
                color: #4e442f;
            }

            .admin-layout {
                display: flex;
            }
            .admin-sidebar {
                width: 230px;
                min-height: 100vh;
                background: #1a2e4f !important;
                color: #fff;
                display: flex;
                flex-direction: column;
                justify-content: space-between;
                position: fixed;
                left: 0;
                top: 0;
                bottom: 0;
                z-index: 100;
                box-shadow: 2px 0 8px rgba(0,0,0,0.07);
            }
            .sidebar-links {
                display: flex;
                flex-direction: column;
                gap: 10px;
                margin-top: 30px;
            }
            .admin-nav-link {
                color: #fff;
                text-decoration: none;
                font-weight: bold;
                padding: 12px 24px;
                border-radius: 6px;
                transition: background 0.2s, color 0.2s;
                font-family: 'Georgia', serif;
                display: flex;
                align-items: center;
                gap: 10px;
            }
            .admin-nav-link.active, .admin-nav-link:hover {
                background-color: #b2903e;
                color: #fff;
            }
            .sidebar-user {
                margin: 30px 0 20px 0;
                padding: 0 24px;
                font-family: 'Georgia', serif;
                font-size: 1em;
                display: flex;
                flex-direction: column;
                gap: 8px;
            }
            .logout-link {
                color: #b2903e;
                text-decoration: none;
                font-weight: bold;
                font-size: 0.95em;
                transition: color 0.2s;
            }
            .logout-link:hover {
                color: #fff;
            }
            .dashboard-main-content {
                margin-left: 250px;
                width: 100%;
                padding: 30px 30px 30px 0;
            }
            
            .user-nav {
                position: absolute;
                right: 1rem;
                top: 50%;
                transform: translateY(-50%);
                display: flex;
                gap: 1rem;
                align-items: center;
            }
            
            .nav-link {
                color: #5a4633;
                text-decoration: none;
                padding: 0.5rem 1rem;
                border-radius: 5px;
                transition: all 0.3s ease;
                font-weight: 500;
                display: flex;
                align-items: center;
                gap: 0.5rem;
            }
            
            .nav-link:hover {
                background: rgba(90, 70, 51, 0.1);
                color: #5a4633;
            }
            
            .cart-link {
                background: linear-gradient(135deg, #b2903e, #9b7e46);
                color: white !important;
            }
            
            .cart-link:hover {
                background: linear-gradient(135deg, #9b7e46, #7d6435);
                color: white !important;
            }
            
            .cart-badge {
                background: #dc3545;
                color: white;
                border-radius: 50%;
                padding: 0.2rem 0.5rem;
                font-size: 0.7rem;
                font-weight: bold;
                margin-left: 0.3rem;
                min-width: 1.2rem;
                text-align: center;
                display: inline-block;
            }
            
            .cart-link-enhanced {
                position: relative;
                display: flex !important;
                align-items: center;
            }
        </style>
    {% endblock %}
</head>
<body>
    <div class="container">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-3">{{ message }}</div>
        {% endfor %}
    </div>

    {% block body %}
        {% if app.request.get('_route') is defined and app.request.get('_route') starts with 'admin_' %}
            <div class="admin-layout">
                <nav class="admin-sidebar">
                    <div class="sidebar-links">
                        <a href="{{ path('admin_dashboard') }}" class="admin-nav-link{% if app.request.get('_route') == 'admin_dashboard' %} active{% endif %}"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                        <a href="{{ path('admin_orders_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_orders' %} active{% endif %}"><i class="fas fa-clipboard-list"></i> Orders</a>
                        <a href="{{ path('admin_returns_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_returns' %} active{% endif %}"><i class="fas fa-undo"></i> Returns</a>
                        <a href="{{ path('admin_disciplines_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_disciplines' %} active{% endif %}"><i class="fas fa-sitemap"></i> Disciplines</a>
                        <a href="{{ path('admin_authors_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_authors' %} active{% endif %}"><i class="fas fa-user-edit"></i> Authors</a>
                        <a href="{{ path('admin_publishers_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_publishers' %} active{% endif %}"><i class="fas fa-building"></i> Publishers</a>
                        <a href="{{ path('admin_books_index') }}" class="admin-nav-link{% if app.request.get('_route') starts with 'admin_books' or app.request.get('_route') starts with 'admin_exemplaire' %} active{% endif %}"><i class="fas fa-book"></i> Books</a>
                        <a href="{{ path('main_site') }}" class="admin-nav-link"><i class="fas fa-globe"></i> Main Site</a>
                    </div>
                    <div class="sidebar-user">
                        <span>Bienvenue, Admin</span>
                        <a href="{{ path('app_logout') }}" class="logout-link"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                    </div>
                </nav>
                <div class="dashboard-main-content">
                    {% block content %}{% endblock %}
                </div>
            </div>
        {% else %}
        <header class="library-header">
            <img src="/library-logo.svg" alt="Library Logo" class="library-logo"/>
            <span class="library-title">Bibliothèque UIASS</span>
            
            {% if app.user %}
                <nav class="user-nav">
                    <a href="{{ path('home') }}" class="nav-link">
                        <i class="fas fa-home"></i> Accueil
                    </a>
                    <a href="/search" class="nav-link">
                        <i class="fas fa-search"></i> Recherche
                    </a>
                    <a href="{{ path('view_cart') }}" class="nav-link cart-link cart-link-enhanced">
                        <i class="fas fa-shopping-cart"></i> Mon Panier
                        {% set cartCount = cart_count() %}
                        {% if cartCount > 0 %}
                            <span class="cart-badge" id="cart-badge">{{ cartCount }}</span>
                        {% endif %}
                    </a>
                    <a href="{{ path('app_logout') }}" class="nav-link">
                        <i class="fas fa-sign-out-alt"></i> Déconnexion
                    </a>
                </nav>
            {% endif %}
        </header>
        <main class="library-main">
                {{ block('content') }}
        </main>
        {% endif %}
        {% if not (app.request.get('_route') is defined and app.request.get('_route') starts with 'admin_') %}
        <footer class="library-footer">
            &copy; {{ "now"|date("Y") }} Bibliothèque UIASS. Tous droits réservés.
        </footer>
        {% endif %}
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        <script>
            // Global cart functions for all pages - attach to window object
            window.updateCartBadge = function(count = null) {
                const badge = document.querySelector('.cart-badge');
                const cartLink = document.querySelector('.cart-link');
                
                if (count === null) {
                    // Get current count and decrement
                    if (badge) {
                        count = parseInt(badge.textContent) - 1;
                    } else {
                        count = 0;
                    }
                }
                
                if (count <= 0) {
                    if (badge) {
                        badge.remove();
                    }
                } else {
                    if (badge) {
                        badge.textContent = count;
                    } else if (cartLink) {
                        // Create new badge
                        const newBadge = document.createElement('span');
                        newBadge.className = 'cart-badge';
                        newBadge.id = 'cart-badge';
                        newBadge.textContent = count;
                        cartLink.appendChild(newBadge);
                    }
                }
            };
            
            window.addToCartGlobal = function(bookId, button) {
                console.log('addToCartGlobal called with:', bookId, button);
                
                const originalContent = button.innerHTML;
                button.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Ajout...';
                button.disabled = true;
                
                fetch(`/add-to-cart/${bookId}`, {
                    method: 'POST',
                    headers: {
                        'X-Requested-With': 'XMLHttpRequest',
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => {
                    console.log('Response received:', response);
                    return response.json();
                })
                .then(data => {
                    console.log('Data received:', data);
                    if (data.success) {
                        // Update badge
                        const badge = document.querySelector('.cart-badge');
                        const currentCount = badge ? parseInt(badge.textContent) : 0;
                        window.updateCartBadge(currentCount + 1);
                        
                        // Visual feedback
                        button.innerHTML = '<i class="fas fa-check"></i> Ajouté !';
                        button.style.backgroundColor = '#28a745';
                        
                        setTimeout(() => {
                            button.innerHTML = originalContent;
                            button.disabled = false;
                            button.style.backgroundColor = '';
                        }, 2000);
                    } else {
                        button.innerHTML = originalContent;
                        button.disabled = false;
                        alert(data.message || 'Erreur lors de l\'ajout au panier');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    button.innerHTML = originalContent;
                    button.disabled = false;
                    alert('Une erreur est survenue: ' + error.message);
                });
            };
            
            // Ensure proper Turbo navigation
            document.addEventListener('turbo:load', function() {
                // Re-initialize any components that need it after SPA navigation
                console.log('Turbo page loaded:', window.location.pathname);
            });
            
            // Handle external links that should force full page reload
            document.addEventListener('turbo:before-visit', function(event) {
                const url = new URL(event.detail.url);
                // Force full reload for admin-to-main or main-to-admin navigation
                if ((url.pathname.startsWith('/admin') && !window.location.pathname.startsWith('/admin')) ||
                    (!url.pathname.startsWith('/admin') && window.location.pathname.startsWith('/admin'))) {
                    event.preventDefault();
                    window.location.href = event.detail.url;
                }
            });
        </script>
    {% endblock %}
</body>
</html>
