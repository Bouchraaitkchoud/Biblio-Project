<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reçu {{ receipt.code }}</title>
    <style>
        body { 
            font-family: 'Helvetica Neue', Arial, sans-serif; 
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .header { 
            text-align: center; 
            margin-bottom: 20px;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
        }
        .header h2 {
            color: #2c3e50;
            margin: 0;
            font-size: 1.5em;
        }
        .receipt-info { 
            margin-bottom: 15px;
            padding: 12px;
            border-radius: 5px;
        }
        .items { 
            margin: 20px 0;
            padding: 12px;
            border-radius: 5px;
        }
        .items h3 {
            border-bottom: 1px dashed #ddd;
            padding-bottom: 8px;
            margin-top: 0;
            color: #2c3e50;
        }
        .item { 
            display: flex; 
            justify-content: space-between; 
            margin: 8px 0;
            padding: 5px 0;
            border-bottom: 1px dotted #eee;
        }
        .footer { 
            margin-top: 25px; 
            text-align: center; 
            font-size: 0.85em;
            color: #7f8c8d;
            border-top: 2px solid #3498db;
            padding-top: 10px;
        }
        .status {
            font-weight: bold;
            color: #27ae60;
        }
        .barcode {
            text-align: center;
            margin: 15px 0;
        }
        .barcode img {
            max-width: 100%;
            height: auto;
        }
        .barcode-text {
            font-size: 0.8em;
            color: #666;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>Reçu de la Bibliothèque</h2>
    </div>
    
    <div class="receipt-info">
        <p><strong>N° de reçu :</strong> {{ receipt.code }}</p>
        <p><strong>Date :</strong> {{ receipt.generatedAt|date('d/m/Y H:i') }}</p>
        <p><strong>Utilisateur :</strong> {{ receipt.order.lecteur.email }}</p>
    </div>
    
    <div class="barcode">
        <img src="data:image/png;base64,{{ barcode }}" alt="Barcode">
        <div class="barcode-text">{{ receipt.code }}</div>
    </div>
    
    <div class="items">
        <h3>Livres empruntés :</h3>
        {% for item in receipt.order.items %}
        <div class="item">
            <span>{{ item.exemplaire.book.title }}</span>
            <span>par {{ item.exemplaire.book.author.nom }}</span>
        </div>
        {% endfor %}
    </div>
    
    <div class="footer">
        <p><span class="status">Statut : {{ receipt.order.status|upper }}</span></p>
        <p>Traité par : {{ receipt.order.processedBy ? receipt.order.processedBy.email : 'EN ATTENTE' }}</p>
        <p>Merci pour votre emprunt !</p>
    </div>
</body>
</html>