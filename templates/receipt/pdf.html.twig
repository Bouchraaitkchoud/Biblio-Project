<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Receipt {{ receipt.code }}</title>
    <style>
        body { 
            font-family: 'Helvetica Neue', Arial, sans-serif; 
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .header { 
            text-align: center; 
            margin-bottom: 20px;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
        }
        .header h2 {
            color: #2c3e50;
            margin: 0;
            font-size: 1.5em;
        }
        .receipt-info { 
            margin-bottom: 15px;
            padding: 12px;
            border-radius: 5px;
        }
        .items { 
            margin: 20px 0;
            padding: 12px;
            border-radius: 5px;
        }
        .items h3 {
            border-bottom: 1px dashed #ddd;
            padding-bottom: 8px;
            margin-top: 0;
            color: #2c3e50;
        }
        .item { 
            display: flex; 
            justify-content: space-between; 
            margin: 8px 0;
            padding: 5px 0;
            border-bottom: 1px dotted #eee;
        }
        .footer { 
            margin-top: 25px; 
            text-align: center; 
            font-size: 0.85em;
            color: #7f8c8d;
            border-top: 2px solid #3498db;
            padding-top: 10px;
        }
        .status {
            font-weight: bold;
            color: #27ae60;
        }
        .barcode {
            text-align: center;
            margin: 15px 0;
        }
        .barcode img {
            max-width: 100%;
            height: auto;
        }
        .barcode-text {
            font-size: 0.8em;
            color: #666;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>Library Receipt</h2>
    </div>
    
    <div class="receipt-info">
        <p><strong>Receipt No:</strong> {{ receipt.code }}</p>
        <p><strong>Date:</strong> {{ receipt.generatedAt|date('d/m/Y H:i') }}</p>
        <p><strong>User:</strong> {{ receipt.cart.user.email }}</p>
    </div>
    
    <div class="barcode">
        <img src="data:image/png;base64,{{ barcode }}" alt="Barcode">
        <div class="barcode-text">{{ receipt.code }}</div>
    </div>
    
    <div class="items">
        <h3>Borrowed Books:</h3>
        {% for book in receipt.cart.books %}
        <div class="item">
            <span>{{ book.title }}</span>
            <span>by {{ book.authorName }}</span>
        </div>
        {% endfor %}
    </div>
    
    <div class="footer">
        <p><span class="status">Status: {{ receipt.cart.status|upper }}</span></p>
        <p>Processed by: {{ receipt.cart.processedBy ? receipt.cart.processedBy.email : 'SYSTEM' }}</p>
        <p>Thank you for your borrowing!</p>
    </div>
</body>
</html>