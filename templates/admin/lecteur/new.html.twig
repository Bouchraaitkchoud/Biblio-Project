{% extends 'admin/layout.html.twig' %}

{% block title %}Nouveau Lecteur{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .lecteur-page-header {
            background: white;
            border-bottom: 3px solid #b2903e;
            padding: 1.5rem 0;
            margin-bottom: 2rem;
        }
        .lecteur-page-header h1 {
            color: #1a2e4f;
            font-size: 1.75rem;
            font-weight: 700;
            margin: 0;
        }
        .lecteur-page-header .breadcrumb-text {
            color: #6c757d;
            font-size: 0.9rem;
            margin-top: 0.25rem;
        }
        .mode-toggle-container {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 2rem;
            border: 1px solid #dee2e6;
        }
        .btn-mode-toggle {
            background: white;
            color: #1a2e4f;
            border: 2px solid #b2903e;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-mode-toggle:hover {
            background: #b2903e;
            color: white;
            transform: translateY(-1px);
            box-shadow: 0 4px 8px rgba(178, 144, 62, 0.2);
        }
        .lecteur-form-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            border: 1px solid #e0e0e0;
            overflow: hidden;
            margin-bottom: 2rem;
        }
        .lecteur-form-card-header {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 1.25rem 1.5rem;
            border-bottom: 2px solid #b2903e;
        }
        .lecteur-form-card-header h5 {
            color: #1a2e4f;
            font-weight: 700;
            font-size: 1.15rem;
            margin: 0;
        }
        .lecteur-form-card-body {
            padding: 2rem;
            background: white;
        }
        .form-label {
            color: #1a2e4f;
            font-weight: 600;
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }
        .form-control {
            border: 1px solid #ced4da;
            border-radius: 6px;
            padding: 0.65rem 0.75rem;
            transition: all 0.2s ease;
        }
        .form-control:focus {
            border-color: #b2903e;
            box-shadow: 0 0 0 0.2rem rgba(178, 144, 62, 0.15);
        }
        .form-text {
            color: #6c757d;
            font-size: 0.875rem;
        }
        .action-buttons {
            background: #f8f9fa;
            padding: 1.25rem 2rem;
            border-top: 1px solid #e0e0e0;
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }
        .btn-save {
            background: linear-gradient(135deg, #b2903e 0%, #d4af37 100%);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.7rem 2rem;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-save:hover {
            background: linear-gradient(135deg, #8d7230 0%, #b2903e 100%);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(178, 144, 62, 0.3);
        }
        .btn-upload {
            background: linear-gradient(135deg, #28a745 0%, #34ce57 100%);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.7rem 2rem;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-upload:hover {
            background: linear-gradient(135deg, #218838 0%, #28a745 100%);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(40, 167, 69, 0.3);
        }
        .btn-cancel {
            background: white;
            color: #6c757d;
            border: 2px solid #dee2e6;
            font-weight: 500;
            padding: 0.7rem 1.5rem;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-cancel:hover {
            background: #6c757d;
            color: white;
            border-color: #6c757d;
        }
        .csv-info-alert {
            background: linear-gradient(135deg, #e8f4f8 0%, #d1ecf1 100%);
            border-left: 4px solid #17a2b8;
            border-radius: 8px;
            padding: 1.25rem;
            margin-bottom: 1.5rem;
        }
        .csv-info-alert h6 {
            color: #0c5460;
            font-weight: 700;
            font-size: 1rem;
            margin-bottom: 0.75rem;
        }
        .csv-info-alert p {
            color: #0c5460;
            margin-bottom: 0.5rem;
        }
        .csv-info-alert code {
            background: rgba(255, 255, 255, 0.8);
            color: #0c5460;
            padding: 0.3rem 0.6rem;
            border-radius: 4px;
            font-size: 0.875rem;
            border: 1px solid rgba(23, 162, 184, 0.2);
        }
        .help-sidebar {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            border: 1px solid #e0e0e0;
            overflow: hidden;
        }
        .help-sidebar-header {
            background: linear-gradient(135deg, #ffc107 0%, #ffb300 100%);
            color: #1a2e4f;
            padding: 1rem 1.25rem;
            font-weight: 700;
            font-size: 1rem;
        }
        .help-sidebar-body {
            padding: 1.5rem;
        }
        .help-sidebar-body h6 {
            color: #1a2e4f;
            font-weight: 700;
            font-size: 0.95rem;
            margin-bottom: 0.75rem;
            margin-top: 1.25rem;
        }
        .help-sidebar-body h6:first-child {
            margin-top: 0;
        }
        .help-sidebar-body ul {
            padding-left: 1.25rem;
            margin-bottom: 0;
        }
        .help-sidebar-body li {
            color: #495057;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
        .help-sidebar-body p {
            color: #6c757d;
            font-size: 0.875rem;
            line-height: 1.5;
        }
    </style>
{% endblock %}

{% block admin_content %}
    <div class="lecteur-page-header">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1><i class="fas fa-user-plus me-2"></i>Nouveau Lecteur</h1>
                <div class="breadcrumb-text">
                    <i class="fas fa-home me-1"></i> Admin / Lecteurs / Nouveau
                </div>
            </div>
            <a href="{{ path('admin_lecteurs_index') }}" class="btn btn-cancel">
                <i class="fas fa-arrow-left me-2"></i>Retour à la liste
            </a>
        </div>
    </div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    <div class="mode-toggle-container">
        <div class="d-flex align-items-center justify-content-between">
            <div>
                <h6 class="mb-1" style="color: #1a2e4f; font-weight: 600;">
                    <i class="fas fa-sliders-h me-2"></i>Mode de saisie
                </h6>
                <p class="mb-0 text-muted small">Choisissez entre la saisie manuelle ou l import CSV</p>
            </div>
            <button type="button" class="btn btn-mode-toggle" id="toggleImportBtn" onclick="toggleImportMode()">
                <i class="fas fa-file-csv me-2"></i>Passer au mode CSV
            </button>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8" id="manualForm">
            <div class="lecteur-form-card">
                <div class="lecteur-form-card-header">
                    <h5><i class="fas fa-edit me-2"></i>Formulaire de saisie manuelle</h5>
                </div>
                <div class="lecteur-form-card-body">
                    {{ form_start(form) }}
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.nom, {label: 'Nom *'}) }}
                            </div>
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.prenom, {label: 'Prenom *'}) }}
                            </div>
                        </div>
                        <div class="mb-3">
                            {{ form_row(form.email, {label: 'Adresse email *', attr: {placeholder: 'exemple@email.com'}}) }}
                            <small class="form-text">L email doit etre unique dans le systeme</small>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.formation, {label: 'Formation', attr: {placeholder: 'Ex: Informatique'}}) }}
                            </div>
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.promotion, {label: 'Promotion', attr: {placeholder: 'Ex: 2024'}}) }}
                            </div>
                        </div>
                        <div class="mb-3">
                            {{ form_row(form.password, {label: 'Mot de passe *', attr: {placeholder: 'Minimum 6 caracteres'}}) }}
                            <small class="form-text">Le mot de passe sera stocke en clair (minimum 6 caracteres)</small>
                        </div>
                </div>
                <div class="action-buttons">
                    <button type="submit" class="btn btn-save">
                        <i class="fas fa-check me-2"></i>Enregistrer le lecteur
                    </button>
                    <a href="{{ path('admin_lecteurs_index') }}" class="btn btn-cancel">
                        <i class="fas fa-times me-2"></i>Annuler
                    </a>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

        <div class="col-lg-8" id="csvImportForm" style="display: none;">
            <div class="lecteur-form-card">
                <div class="lecteur-form-card-header">
                    <h5><i class="fas fa-file-csv me-2"></i>Import de lecteurs via CSV</h5>
                </div>
                <div class="lecteur-form-card-body">
                    <div class="csv-info-alert">
                        <h6><i class="fas fa-info-circle me-2"></i>Format du fichier CSV requis</h6>
                        <p class="mb-2">Votre fichier CSV doit contenir les colonnes suivantes (dans cet ordre precis) :</p>
                        <p class="mb-0"><code>Nom, Prenom, Code d admission, Etablissement, Formation, Promotion, Email, Mot de passe</code></p>
                    </div>
                    <form action="{{ path('admin_lecteurs_import_csv') }}" method="post" enctype="multipart/form-data">
                        <div class="mb-4">
                            <label for="csvFile" class="form-label">
                                <i class="fas fa-cloud-upload-alt me-2"></i>Selectionner un fichier CSV
                            </label>
                            <input type="file" class="form-control form-control-lg" id="csvFile" name="csv_file" accept=".csv" required>
                            <small class="form-text"><i class="fas fa-file-alt me-1"></i>Formats acceptes : .csv uniquement</small>
                        </div>
                        <input type="hidden" name="_token" value="{{ csrf_token('csv_import') }}">
                </div>
                <div class="action-buttons">
                    <button type="submit" class="btn btn-upload">
                        <i class="fas fa-upload me-2"></i>Importer les lecteurs
                    </button>
                    <a href="{{ path('admin_lecteurs_index') }}" class="btn btn-cancel">
                        <i class="fas fa-times me-2"></i>Annuler
                    </a>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="help-sidebar">
                <div class="help-sidebar-header">
                    <i class="fas fa-question-circle me-2"></i>Aide et Informations
                </div>
                <div class="help-sidebar-body">
                    <h6><i class="fas fa-asterisk me-2" style="font-size: 0.7rem; color: #dc3545;"></i>Champs obligatoires</h6>
                    <ul>
                        <li>Nom du lecteur</li>
                        <li>Prenom du lecteur</li>
                        <li>Adresse email (doit etre unique)</li>
                        <li>Mot de passe (minimum 6 caracteres)</li>
                    </ul>
                    <h6><i class="fas fa-lock me-2"></i>Securite du mot de passe</h6>
                    <p>Le mot de passe sera stocke en clair dans la base de donnees.</p>
                    <h6><i class="fas fa-file-csv me-2"></i>Import en masse</h6>
                    <p>Utilisez le mode CSV pour creer plusieurs lecteurs simultanement.</p>
                    <h6><i class="fas fa-lightbulb me-2"></i>Conseil</h6>
                    <p class="mb-0">Verifiez bien les informations avant de valider, notamment l email.</p>
                </div>
            </div>
        </div>
    </div>

    <script>
        function toggleImportMode() {
            const manualForm = document.getElementById('manualForm');
            const csvForm = document.getElementById('csvImportForm');
            const toggleBtn = document.getElementById('toggleImportBtn');
            if (manualForm.style.display === 'none') {
                manualForm.style.display = 'block';
                csvForm.style.display = 'none';
                toggleBtn.innerHTML = '<i class="fas fa-file-csv me-2"></i>Passer au mode CSV';
            } else {
                manualForm.style.display = 'none';
                csvForm.style.display = 'block';
                toggleBtn.innerHTML = '<i class="fas fa-edit me-2"></i>Passer au mode manuel';
            }
        }
    </script>
{% endblock %}
